# 复制为 .env 或 .env.mock 使用。

# 真实后端：/api 经 vite 代理转发（vite.config.ts 已配置 /api -> http://localhost:8888）
VITE_API_BASE=/api

# MSW Mock 作用域：
#   auth —— dev 模式默认：认证后端未落地，仅认证走 mock，其余走真实网关
#   all  —— 全量 mock（配合 npm run dev:mock，无需后端）
# 留空则不启用 MSW。
# VITE_MOCK_SCOPE=auth
# VITE_MOCK_SCOPE=all
